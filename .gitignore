.gradle
.idea
local.properties
*.iml
build/
app/build/

*.hprof
.claude/

# Signing keystores — never commit these
*.jks
*.keystore

# Sentry — DSN/auth-token live in env vars (release.yml) and BuildConfig at
# build time. The wizard drops these locally; don't let them commit.
sentry.properties
.sentryclirc
sentry-wizard

# Gradle daemon config is machine-local
gradle/gradle-daemon-jvm.properties
IMPROVEMENTS.md
