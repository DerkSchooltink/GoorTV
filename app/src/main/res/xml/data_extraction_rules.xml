<?xml version="1.0" encoding="utf-8"?>
<!-- Explicit Android 12+ counterpart to allowBackup="false": exclude all app
     data from cloud backup and device-to-device transfer. The Room database
     holds user source credentials (Keystore-encrypted, but the key is
     device-bound so transferred ciphertext would be undecryptable anyway). -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
